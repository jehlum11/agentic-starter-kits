apiVersion: v1
kind: Service
metadata:
  name: a2a-crew-agent
  labels:
    app: a2a-crew-agent
    app.kubernetes.io/part-of: a2a-langgraph-crewai
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: a2a-crew-agent
