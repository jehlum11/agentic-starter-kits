apiVersion: v1
kind: Service
metadata:
  name: langgraph-outdoor-activity-agent
  labels:
    app: langgraph-outdoor-activity-agent
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: langgraph-outdoor-activity-agent
